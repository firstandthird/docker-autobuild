#!/bin/bash

if [[ "$ONLY_TAGS" == "1" ]]; then
  exit 0
fi

if [[ -n "$ONLY_BRANCH" && "$ONLY_BRANCH" != "$GITHUB_BRANCH" ]]; then
  echo "looking for $ONLY_BRANCH, got $GITHUB_BRANCH"
  exit 0
fi

export TAG_BRANCH=1
export TAG_PREFIX_BRANCH=1
bash /scripts/lib/build
