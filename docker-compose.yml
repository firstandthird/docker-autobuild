app:
  build: '.'
  dockerfile: 'DockerfileDev'
  command: 'npm run dev'
  environment:
    SECRET: 'secret'
    GITHUB_TOKEN:
    VIRTUAL_HOST:
    SLACK_HOOK:
    RAPPTOR_CONFIG_URL:
  volumes:
    - './:/home/app/src'
    - '/repos:/repos'
    - '/var/run/docker.sock:/var/run/docker.sock'
#     - '$HOME/.docker:/home/app/.docker'
